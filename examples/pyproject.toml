[project]
name = "skyfield-examples"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "matplotlib",
    "scipy",
    "pytz",
    "skyfield",
    "requests>=2.32.5",
]

[tool.uv.sources]
skyfield = { path = "..", editable = true }
